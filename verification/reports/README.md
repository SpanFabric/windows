# Verification reports

Non-executable reports/evidence metadata live here and are excluded from the review-subject digest by fixed policy. A file named `PASS` has no authority by itself. Authoritative evidence is referenced from `verification/state.yaml` and must bind producer, role, commit SHA, review-subject digest and timestamp.
